# QUESTION 1
# Write a method that takes in an array of numbers and returns an array with every number doubled.
# Then run the method and print the result.

array = [1, 2, 3, 4, 5,]
index = 0
def Array(array)
    
    while index < array.length
        array(index)
        index = index + 1
    end
    return array 
end

  pp Array(array)
# welp I'm spinning my tires, help
